# GitHub repo to import data.json and uploads from at build time
# Leave blank to skip remote import
GITHUB_OWNER=
GITHUB_REPO=
GITHUB_TOKEN=
GITHUB_BRANCH=main
# Paths inside the repo (defaults shown)
GITHUB_DATA_PATH=public/data.json
GITHUB_UPLOADS_DIR=public/uploads
